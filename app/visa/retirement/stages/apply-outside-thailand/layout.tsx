import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Retirement Visa Checklist - Apply outside Thailand | Thai Visa Checklist",
  description:
    "Retirement visa checklist for applying outside Thailand: key rules, required documents, travel details, and post-approval steps.",
};

export default function RetirementStageOneLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
