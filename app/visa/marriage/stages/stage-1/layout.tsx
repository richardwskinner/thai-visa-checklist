import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Marriage Visa Stage 1 Checklist - Apply Abroad (eVisa) | Thai Visa Checklist",
  description:
    "Stage 1 marriage visa checklist for Thailand: apply abroad for a Non-Immigrant O (Thai spouse/family) via Thai eVisa, with required documents and travel details.",
};

export default function MarriageStageOneLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
